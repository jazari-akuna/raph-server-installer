# enrol — no secret material. All config is via env in compose.
#
# This file exists only so the compose `env_file:` directive (if added
# later) has a target. Currently unused by the committed compose. Safe to
# leave empty.
